import { Plugin } from 'vite';
import { SiteConfig } from './config';
export declare function createVitePressPlugin(root: string, siteConfig: SiteConfig, ssr?: boolean, pageToHashMap?: Record<string, string>, clientJSMap?: Record<string, string>): Plugin[];
